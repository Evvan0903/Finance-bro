# Optional. The app uses a rule-based analyst summary when this is blank.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Required for live SEC access. Identify the application and provide a monitored contact.
SEC_USER_AGENT=SEC Financial Report Agent your-email@example.com
